LangChain-Fundamentals,RAG,Agents
LangChain-0.3 version we are learning
Open Source to use and develop LLM

Searching : KeyWord Search ,  Semantic Search
NLU:Natural Language Understanding
Context Aware Text Generation Capability 

How Semantic Search works: so their is a query and answers so the answers are converted to embeddings(n dims vectors{set of numbers}) Embeddings methods like [Word2Vec , Doc2vec , BERT] and the query is also converted to vector using embedding then consine similarity is found to see if the closest angle(gives the most similar meaning answer)
getting answers from LLM are called inference

Langchain provides built-in functionality
concepts of chains(pipelines)
model agnostic development(irrespective of any model)
Complete Ecosystem
Memory and state handling

Alternatives to Langchain:LlamaIndex and HayStack

Components of LangChain
1.Models
2.Prompts
3.chains
4.Memory
5.Indexes
6.Agents

Models
interface to interact with AI
LLM API

in LangChain we can talk to 2 Types
1.Language Models: text->LLM->text    (Chat Models)
2.Embedding Models: text->LLM->vector (use:Semantic Search) (Embedding Models) in openai in text-embedding-small model the max dims is 1536 and for text-embedding-large model the max dims is 3072
closed source models:OpenAI and Antropic and Google deep mind
open source models:By HuggingFace

Language Models are of two types
1.LLM : General Purpose  models
2.Chat Models : Used for conversational tasks


LLMs (Large Language Models)
----------------------------
General-purpose models used for raw text generation.
They take a string (plain text) as input and return a string (plain text) as output.
These are traditionally older models and are not used much now.

Chat Models
------------
Language models specialized for conversational tasks.
They take a sequence of messages as input and return chat messages as output
(instead of using plain text).

These are traditionally newer models and are used more compared to basic LLMs.


Comparison: LLMs vs Chat Models
================================

1. Purpose
   - LLMs: Free-form text generation
   - Chat Models: Optimized for multi-turn conversations

2. Training Data
   - LLMs: General text corpora (books, articles)
   - Chat Models: Fine-tuned on chat datasets
     (dialogues, user-assistant conversations)

3. Memory & Context
   - LLMs: No built-in memory
   - Chat Models: Supports structured conversation history

4. Role Awareness
   - LLMs: No understanding of "user" and "assistant" roles
   - Chat Models: Understands "system", "user", and "assistant" roles

5. Example Models
   - LLMs: GPT-3, Llama-2-7B, Mistral-7B, OPT-1.3B
   - Chat Models: GPT-4, GPT-3.5-turbo, Llama-2-Chat,
                  Mistral-Instruct, Claude

6. Use Cases
   - LLMs: Text generation, summarization, translation,
            creative writing, code generation
   - Chat Models: Conversational AI, chatbots, virtual assistants,
                   customer support, AI tutors

Open-source language models are freely available AI models that can be downloaded, modified,
fine-tuned, and deployed without restrictions from a central provider. Unlike closed-source
models such as OpenAI's GPT-4, Anthropic's Claude, or Google's Gemini, open-source models
allow full control and customization.
 
---
 
Feature          | Open-Source Models                              | Closed-Source Models
-----------------|-------------------------------------------------|--------------------------------------
Cost             | Free to use (no API costs)                      | Paid API usage (e.g., OpenAI charges per token)
Control          | Can modify, fine-tune, and deploy anywhere      | Locked to provider's infrastructure
Data Privacy     | Runs locally (no data sent to external servers) | Sends queries to provider's servers
Customization    | Can fine-tune on specific datasets              | No access to fine-tuning in most cases
Deployment       | Can be deployed on on-premise servers or cloud  | Must use vendor's API
 
---
 
Some Famous Open Source Models
 
Model            | Developer   | Parameters  | Best Use Case
-----------------|-------------|-------------|----------------------------------------------
LLaMA-2-7B/13B/70B | Meta AI  | 7B - 70B    | General-purpose text generation
Mixtral-8x7B     | Mistral AI  | 8x7B (MoE)  | Efficient & fast responses
Mistral-7B       | Mistral AI  | 7B          | Best small-scale model (outperforms LLaMA-2-13B)
Falcon-7B/40B    | TII UAE     | 7B - 40B    | High-speed inference
BLOOM-176B       | BigScience  | 176B        | Multilingual text generation
GPT-J-6B         | EleutherAI  | 6B          | Lightweight and efficient
GPT-NeoX-20B     | EleutherAI  | 20B         | Large-scale applications
StableLM         | Stability AI| 3B - 7B     | Compact models for chatbots

HuggingFace is the largest repositories of open-source LLM's
two ways to use it
1.Running locally
2.Using HF inference API

Disadvantages

Disadvantage               | Details
---------------------------|------------------------------------------------------------------------
High Hardware Requirements | Running large models (e.g., LLaMA-2-70B) requires expensive GPUs.
Setup Complexity           | Requires installation of dependencies like PyTorch, CUDA, transformers.
Lack of RLHF               | Most open-source models don't have fine-tuning with human feedback,
                           | making them weaker in instruction-following.
Limited Multimodal         | Open models don't support images, audio, or video like GPT-4V.
Abilities                  |


Prompts
Prompt engineering
input to llm are prompts

1.Dynamic and Resuable Prompts
2.Role Based Prompts(system prompts and user prompts)
3.Few Shot Prompting( give it examples and what type of result to expect)


chains
help to build pipelines
previous output is automatically the input of next stage

types of chains
1.Parallel chain
2.Conditional chain

Indexes
They Connect your application to external Knowledge: pdf , websites ,  Databases

1.Document Loaders
2.Text Splitters
3.Vector Store 
4.Retreivers

Memory
LLM calls are stateless

Types of Memory

ConversationBufferMemory: Stores a transcript of recent messages. Great for short chats but can grow large quickly.
ConversationBufferWindowMemory: Only keeps the last N interactions to avoid excessive token usage.
Summarizer-Based Memory: Periodically summarizes older chat segments to keep a condensed memory footprint.
Custom Memory: For advanced use cases, you can store specialized state (e.g., the user's preferences or key facts about them) in a custom memory class.

Agents

LLM with tools calls( they do the work rather than only giving information)
they have resoning Capability
using Chain-of-thought prompting 


temperature is a parameter that controls the randomness of a language model's output. It affects how creative or deterministic the responses are.

- Lower values ( 0.0 - 0.3 ) -> More deterministic and predictable.
- Higher values ( 0.7 - 1.5 ) -> More random, creative, and diverse.

---

Use Case                                          | Recommended Temperature
--------------------------------------------------|------------------------
Factual answers (math, code, facts)               | 0.0 - 0.3
Balanced response (general QA, explanations)      | 0.5 - 0.7
Creative writing, storytelling, jokes             | 0.9 - 1.2
Maximum randomness (wild ideas, brainstorming)    | 1.5+